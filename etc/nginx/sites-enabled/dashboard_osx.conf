server {
    listen 8000 default_server;
    server_name _; #status.julialang.org;
    access_log /usr/local/var/log/nginx/dashboard.access.log;
    error_log /usr/local/var/log/nginx/dashboard.error.log;

    location /static/ {
        alias /Users/sabae/src/status.julialang.org/www/dashboard/sitestatic/;
        expires 30d;
    }

    location /media/ { # MEDIA_URL
        alias /home/sabae/src/status.julialang.org/www/dashboard/sitestatic/; # MEDIA_ROOT
        expires 30d;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/dashboard.socket;
    }
}
